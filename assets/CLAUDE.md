# assets/

> L2 | 父级: CLAUDE.md

成员清单
4d53de244676405.699d0d046c4dc.webp: 雪夜背景预设资源，被 `src/backgrounds.js` 消费。
96bdad248714663.69fdafdc31e48.webp: AI 面孔背景预设资源，被 `src/backgrounds.js` 消费。
aurora-glass-ball.webp: 默认极光玻璃球背景预设资源，被 `src/backgrounds.js` 作为首屏背景。
background-black.png: 黑色纯色背景预设资源，被 `src/backgrounds.js` 消费。
background-white.png: 白色纯色背景预设资源，被 `src/backgrounds.js` 消费。
wwdc26-wallpaper-mac.webp: 第二张 WWDC26 背景预设资源，由桌面 PNG 压缩到 2400px WebP 后被 `src/backgrounds.js` 消费。

法则: 背景资源必须体积可控、可被 manifest 明确引用；新增或替换预设时同步本地图与 `src/backgrounds.js`。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
